<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="ImageDeduper.App.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:vm="using:ImageDeduper.App.ViewModels"
    mc:Ignorable="d">
    <Grid Padding="12" RowSpacing="12" ColumnSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" HorizontalAlignment="Right" Orientation="Horizontal" Spacing="8">
            <muxc:AppBarButton Icon="Folder" Label="{Binding UiText.CommandFolder}" Command="{Binding BrowseFolderCommand}" />
            <muxc:AppBarButton Icon="Play"
                               Label="{Binding UiText.CommandStart}"
                               Command="{Binding StartCommand}"
                               IsEnabled="{Binding IsStartEnabled}" />
            <muxc:AppBarButton Icon="Stop" Label="{Binding UiText.CommandStop}" Command="{Binding StopCommand}" />
            <muxc:AppBarButton Icon="Setting"
                               Label="{Binding UiText.CommandSettings}"
                               Click="OnSettingsClicked"
                               IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBooleanConverter}}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="0" Spacing="12">
            <Border Background="#11111111" CornerRadius="12" Padding="16">
                <StackPanel Spacing="4">
                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding UiText.SourceFolderTitle}" />
                    <TextBlock TextWrapping="WrapWholeWords" Text="{Binding SelectedFolderDisplay}" />
                    <TextBlock Foreground="Gray" Text="{Binding StatusMessage}" />
                    <StackPanel Margin="0,12,0,0" Spacing="8"
                                Visibility="{Binding ResumePromptVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock FontWeight="SemiBold" Text="{Binding UiText.ResumeDetectedTitle}" />
                        <TextBlock TextWrapping="Wrap"
                                   Foreground="Gray"
                                   Text="{Binding UiText.ResumeDetectedDescription}" />
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button Content="{Binding UiText.ResumeButtonLabel}" Command="{Binding KeepResumeCommand}" />
                            <Button Content="{Binding UiText.StartOverButtonLabel}" Command="{Binding DiscardResumeCommand}" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Background="#11111111" CornerRadius="12" Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding PhaseText}" />
                    <ProgressBar Minimum="0" Maximum="100" Height="18" CornerRadius="8"
                                 Foreground="{StaticResource ProgressBarFillBrush}"
                                 Background="{StaticResource ProgressBarTrackBrush}"
                                 Value="{Binding ProgressValue}" />
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="{Binding ProgressDetail}" />
                            <TextBlock Text="{Binding ProgressPercentText}" Foreground="Gray" />
                        </StackPanel>
                        <TextBlock Text="{Binding UiText.EtaLabel}" Margin="12,0,4,0" Grid.Column="1" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding EtaText}" Grid.Column="2" VerticalAlignment="Center" />
                    </Grid>
                </StackPanel>
            </Border>

            <Border Background="#11111111" CornerRadius="12" Padding="16">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Spacing="24">
                    <StackPanel>
                        <TextBlock FontSize="14" Foreground="Gray" Text="{Binding UiText.TotalImagesLabel}" />
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding TotalSources}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock FontSize="14" Foreground="Gray" Text="{Binding UiText.BaseProcessedLabel}" />
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding ProcessedBase}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock FontSize="14" Foreground="Gray" Text="{Binding UiText.MovedLabel}" />
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding MovedDuplicates}" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock FontSize="14" Foreground="Gray" Text="{Binding UiText.ElapsedLabel}" />
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="{Binding ElapsedText}" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>

        <Border Grid.Row="1" Grid.Column="1" Background="#11111111" CornerRadius="12" Padding="12">
            <StackPanel Spacing="12">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding UiText.LiveLogsTitle}" Margin="0,0,0,4" />
                    <ScrollViewer x:Name="LogsScrollViewer"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  MaxHeight="320">
                        <ItemsControl ItemsSource="{Binding Logs}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:LogEntryViewModel">
                                    <TextBlock Text="{x:Bind Display}"
                                               TextWrapping="WrapWholeWords"
                                               Margin="0,0,0,6"
                                               IsTextSelectionEnabled="True" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Grid.ColumnSpan="2" Background="#0F000000" CornerRadius="8" Padding="12,6,12,12">
            <TextBlock Text="{Binding UiText.FooterDescription}" TextWrapping="Wrap" />
        </Border>
    </Grid>
</Page>
